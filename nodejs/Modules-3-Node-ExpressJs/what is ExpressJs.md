Express js used as a framework of node js 
Express js provides its own structures 
EXpress js support MVC architectures to create a ORM models inside of node js 
Express js is support ORM(object relational mapping) model to performed CRUD operations using mySQl and mongoDB
MVC stands for Model | View | Controller 
MVC is an structures i.e is support Model | view | controller
Express js is used as framework.

Advantage Express js ....

  a) fast loading 
  b) used or create a REST API 
  c) using express create a routing (navigate one page to another page)
  d) using express create a ORM model 
  e) using express create a middleware (user authenticate) while login systems 
  f) using express body-Parser (send data via form)
  g) using express js node js provides some template engine 
       a) ejs (embeded javascript) .ejs 
       b) pug (.pug)
  h) using express js we can send res or req using some methods ... 

     a) get()
         get the response in formate of 200 and get data (read | fetch )
     b) post()
         post the response in formate of 200 (insert | update | login )
     c) put()
         put the response in formate of 200 (update)
     d) delete()
         delete the response in formate of 200 (delete)


How to install express 
  examples : npm install express
