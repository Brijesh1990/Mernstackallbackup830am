what is sql ?
 sql stands for structured query language 
 sql is used to create database and table structured 
 sql is used to insert , update and delete data  
 sql is case insestive language
 examples : INSERT , insert 
what is difference between SQL and MYSql

  SQL ....
     sql stands for structured query language 
     sql is used to create database and table structured 
     sql is used to insert , update and delete data  
     sql is case insestive language
     examples : INSERT , insert 

  MySql ....
     Mysql is a database where we can stored an information about tables in         form of rows and columns.
     database is used to stored an informations of users i.e called database  
     Mysql is a case-senstive.  

     examples : 1) mysql_query()
                2) mysql_fetch_array()
                3) mysql_fetch_object()
                4) mysql_fetch_assoc()
                5) mysql_num_rows()    
                5) con=mysql_connect("localhost","root,"","dbname");  

 
 sql used some commands or query....
 a) DDL(data definition language) 
 b) DML(data manupulation language)
 c) DQL(data query language)
 d) TCL(trasactional query language)   


 a) DDL(data definition language)
      examples : ddl involved to create database strucrures and table structures 
                 a) create b) alter c) rename d) truncate e) drop
                
                 1) how to create database 
                    create database databasename;

                 2) how to create table  
                    create table tablename
                    (
                     columnname int size auto_increment primary key,
                     columnname varchar(0-255),
                     columnname text,
                     columnname varachar(255),
                     price float,
                     datetime datetime,
                     date date,
                     phone bigInt
                    )


examples : 

CREATE TABLE `flipkart`.`contact` (
 contactid int auto_increment primary key,
 name varchar(255),
 email varchar(255),
 phone bigInt,
 subject varchar(255),
 message text

);
 

CREATE TABLE `flipkart`.`categories` (
  
categoryid int primary key  auto_increment,
categoryname varchar(255)

);


2) alter : alter is used to add | modify or update any columnname
examples : 

 alter table flipkart.contact change phone mobile bigInt;
 alter table flipkart.contact add added_date varchar(255);
 Alter table flipkart.contact add photo varchar(255) after mobile;

3) rename : rename is used to change name of table

  rename table flipkart.categories to flipkart.flip_categories
  rename table flipkart.contact to flipkart.flip_contact
    



4) truncate : truncate is used to empty all data or delete all data from tables
           NOTE : after truncate we can not rollback data

            truncate table flipkart.flip_categories;


5) drop : drop is used to delete database and its structures also 
          note : after drop we can not rollback structures 

          drop is also used to delete tables structures after drop we can not rollback table structure 

          example : drop database  flipkart; 
          example: drop table flipkart.flip_categories;  


DML : data manupulation language 

      a) insert :  

       1) insert into flipkart.flip_categories values(null,'Electronics'),(null,'Furnitures'),(null,'Mens shirts'),(null,'Womens shirt')      

       2) insert  into flipkart.flip_categories(categoryname) value('kids items')

       3) insert  into flipkart.flip_categories(categoryname) value('womens items'),('Mobile'),('furnitures') 

      b)delete : delete a all rows | particular rows after delete we can rollback data  

                  a) delete all data 
                     delete from flipkart.flip_categories;
                  b) delete from flipkart.flip_categories where categoryid=3;
                  c) delete from flipkart.flip_categories where categoryname='Mobile';
                  d) delete from flipkart.flip_categories where categoryid in (1,3);    
                  e) delete from flipkart.flip_categories where categoryid between 51 and 1000;  


      4) update : update flipkart.flip_employee set  password='bhumika$$1234',mobile='635878987',salary=22500,address='250 feet ring road jetpur' where empid=6;



   3) DQL : data query language 

            select : 

            a) select * from flipkart.flip_employee; //select all rows   
            b) select * from flipkart.flip_employee where empid=6; 
            c) select * from flipkart.flip_employee where name='bhumika'
            d) select name,mobile,salary from flipkart.flip_employee where empid=6 
            e) select * from flipkart.flip_employee where empid in (1,4,6)  
            f) select * from flipkart.flip_employee where empid between 3 and 10;
            g) select * from flipkart.flip_employee where empid limit 0,100;
            h) select * from flipkart.flip_employee where empid limit 4,2; 
            

           order by and group by ....

           order by : filter or select data in assending or decending order ...

                      a) select * from flipkart.flip_employee order by name asc;
                      b) select * from flipkart.flip_employee order by name;
                      c) select * from flipkart.flip_employee order by name desc;

 
           group by : group by select group of columns and applied sum of on group of columns

                  a) select sum(salary),department from flipkart.flip_employee group by department; 

                  b) select avg(salary),department from flipkart.flip_employee group by department; 



       SQL function :
                There are two types of sql function ...
                a) scalar function 
                   1) first 2) last 3) lcase 4) ucase
                   1) select first from flipkart.flip_employee
                   2) select last from flipkart.flip_employee
                   3) select lcase(name) from flipkart.flip_employee
                   4) select ucase(name) from flipkart.flip_employee 

                b) aggrigate function
 
                   1)sum 2)avg 3)max 4)min 5)count   

                   1)select sum(salary) from flipkart.flip_employee;
                   2)select avg(salary) from flipkart.flip_employee;
                   3)select max(salary) from flipkart.flip_employee;
                   4)select min(salary) from flipkart.flip_employee;
                   5)select count(empid) from flipkart.flip_employee; 


                 Note : how to find second highest salary

                 applied subquery ....(query within another i.e called subquery)

                 select max(salary) from flipkart.flip_employee where salary<(select max(salary) from                  flipkart.flip_employee); 



  4) TCL : transaction query language 

            1) commit 
                 start transaction;
delete from flipkart.flip_employee where empid=1;
commit;
            2) rollback     

            start transaction;
select * from flipkart.flip_employee where empid=1;
rollback;


                 
       Key constraints ..                 

       key contsraints is set a limit on tables ...

       types of key constraints ...
       a) primary key(pk)
       b) foreign key (fk)
       c) unique key (uk)
       d) compound key (ck)   

       primary key : A pk is provides only one times in a tables 
                     A pk never returns a null value
                     A pk provides a unique values 
                     A pk is always auto_increments

 CREATE TABLE `flipkart`.`contact` (
 contactid int auto_increment primary key,
 name varchar(255),
 email varchar(255),
 phone bigInt,
 subject varchar(255),
 message text

);


       unique key :  A uk is provides more than one times in a tables 
                     A uk only one times returns a null value
                     A pk provides a unique values 
                     A pk is always auto_increments

                    alter TABLE flipkart.flip_employee ADD UNIQUE(`mobile`)
                    alter TABLE flipkart.flip_employee ADD UNIQUE(`email`)


    foreign key : foreign key is used to provides relationship between one tables to another tables 
                  fk is more than one time in tables
                  fk is never return null values

                  tbl_college

                collegeid     collegename  
                1             marwadi  
                2             vvp 
                3             darshan

                tbl_students  
 
                studenteid    studentname  
                1             kishan  
                2             divesh 
                3             milan


                tbl_teacher

                teacher_id(pk)  collegeid(fk)   studnetid(fk)  name  age   mobile department  address

                1                3               2             brijesh 34  954555  IT          rjt         
                

 

     

                     
                                       


 